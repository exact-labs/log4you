{
	"name": "log4you",
	"version": "0.2.0",
	"description": "A simple and easy to use logger",
	"keywords": [
		"logger"
	],
	"type": "module",
	"repository": "https://github.com/exact-labs/log4you",
	"license": "MIT",
	"author": "theMackabu",
	"main": "dist/logger.js",
	"types": "dist/logger.d.ts",
	"scripts": {
		"build": "rimraf ./dist/ && esbuild src/logger.ts src/colors.ts --minify --format=esm --outdir=dist && tsc",
		"test": "node tests/test.js"
	},
	"devDependencies": {
		"rimraf": "^2.6.2",
		"typescript": "^5.1.6"
	},
	"dependencies": {
		"@types/node": "^20.4.5",
		"esbuild": "^0.18.17"
	}
}
